package lab9

/*Створити клас таким чином, щоб він відстежував кількість існуючих
у даний момент об’єктів та об’єм оперативної пам’яті, який вони займають.
Вказівка: застосувати статичні компоненти класу.*/

fun main() {
    // Цикл, який повторюється 10000 разів.
    repeat(10000) {
        // Створення об'єкту класу MyClass.
        val obj = MyClass()

        // Виклик функції doSomething() з поточним об'єктом.
        obj.doSomething()
    }
}